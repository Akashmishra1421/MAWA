---
- name: Deploy to tomcat
  hosts: web
  become: yes
  tasks:
   - name: Copy war File
     copy:
      src: "/var/snap/jenkins/4870/workspace/MAWA/target/meowansible.war"
      dest: "/opt/tomcat/webapps/meowansible.war"
      remote_src: no
   - name: Restart tomcat
     systemd:
      name: tomcat
      state: restarted
